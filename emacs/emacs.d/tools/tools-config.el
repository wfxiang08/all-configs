;; -*- emacs-lisp -*-

;; -------------------- tools-config --------------------

;; load shell-mode settings
(require 'shell-settings)

;; load help-mode settings
(require 'help-settings)

;; load dired-x mode
(require 'dired-x-settings)

;; load xcscope mode
(require 'xcscope-settings)

(provide 'tools-config)
